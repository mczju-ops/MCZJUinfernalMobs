# 炒鸡怪特殊掉落总开关
# 各等级池子放在 loot/ 文件夹下：loot/1.yml、loot/2.yml、loot/3.yml … 每级一个文件，按权重抽
enable: true
replace-vanilla-drops: true

# 战利品轮换：带 rotation-set 的项仅在对应月份可掉落，未配置的项不受影响
rotation:
  enable: true
  sets: 3
